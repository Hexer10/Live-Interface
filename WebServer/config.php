<?php

define('WS_URL', 'wss://example.com'); //The websocket URL (and port if not 80 or 443).

define('RCON_IP', 'localhost'); //The RCON(Game Server) IP Address.
define('RCON_PASSWORD', 'mypassword'); //The Server's RCON password.

define('WEBAPI_KEY', ''); //The webapi key found here: https://steamcommunity.com/dev/apikey

define('DEVELOPER_MODE', 0); //Enable (set to 1) only for debugging porpoises.